async def cache_manager(cache_file_path):
    '''
    if cache_file_path.exists():
        with open(cache_file_path, 'r', encoding='utf-8') as f:
            cache_data = json.load(f)
        if cache_data == {}:
            return
        else:
            pass
    else:
        return
    for num in cache_data.items():
    '''
